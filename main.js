const calculate = (e) => {
  const inputValue = e.target.value;
  const regex = /[-\/\*\+]/;
  const numA;
  const numB;
  const operator;
};

document.getElementById("inputValue").addEventListener("change", calculate);
